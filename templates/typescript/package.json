{
  "name": "orbflightsearch",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-silent": "jest --collectCoverage=false --reporters jest-silent-reporter",
    "test": "jest --clearCache && jest",
    "clean": "rm -rf coverage dist",
    "build": "npm run clean && tsc && echo 'Build Success'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.11.5",
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "jest": "^24.9.0",
    "jest-when": "^2.7.0",
    "jest-silent-reporter": "^0.2.1",
    "lambda-local": "^1.6.3",
    "nock": "^11.7.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "@hapi/joi": "^16.1.8",
    "@types/properties-reader": "0.0.1",
    "axios": "^0.19.0",
    "logplease": "^1.2.15",
    "node-json-transform": "^1.0.19",
    "object-mapper": "^6.0.1",
    "path": "^0.12.7",
    "properties-reader": "^0.3.1",
    "request": "^2.88.0"
  },
  "jest": {
    "verbose": true,
    "testEnvironment": "node"
  }
}
